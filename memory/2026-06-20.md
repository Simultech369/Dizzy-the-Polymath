# 2026-06-20

- Independent review remediation was authorized across the focused `main..experiments` delta.
- Queue recovery now retries retained notification claims periodically with bounded backoff and operator-visible status; DLQ enqueue and marker persistence are atomic.
- SQLite idempotency now binds replays to an operation fingerprint, including the requested creation effect.
- Review-provider uploads block redirects, require explicit `.env` loading, and reject ambiguous base URLs.
- Dependency-impact governance now validates every structured reconciliation row. The runtime floor is Node 20.18.1.
- Added a reversible, operator-rated divergent-synthesis evaluation harness. It does not call providers or write memory autonomously.
- `npm audit fix` remained blocked by the external approval/usage gate; dependency advisories were not silently rewritten around that constraint.
- Dashboard route policy and read-only data access were isolated in `lib/dashboard.mjs`; disabled mode skips module loading, module failure degrades only dashboard routes, and public trust zones are denied.
